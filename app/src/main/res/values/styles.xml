<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="CalculatorButtonBig" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_marginStart">2dp</item>
        <item name="android:layout_marginEnd">2dp</item>
        <item name="android:enabled">false</item>
        <item name="android:textSize">30sp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="backgroundTint">@color/colorNumbers</item>
    </style>
    <style name="CalculatorButtonIcon" parent="Widget.MaterialComponents.Button.Icon">
        <item name="android:layout_marginStart">2dp</item>
        <item name="android:layout_marginEnd">2dp</item>
        <item name="android:enabled">false</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="backgroundTint">@color/colorDel</item>
    </style>
    <style name="CalculatorButtonSmall" parent="CalculatorButtonBig">
        <!--    <style name="CalculatorButton" parent="TextAppearance.AppCompat.Button">-->
        <item name="android:textSize">22sp</item>
        <item name="backgroundTint">@color/colorOperators</item>
    </style>

</resources>
